name: Beeline DMC Data Extractor Auto-Build

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Сборка скрипта
    runs-on: ubuntu-latest

    steps:
      - name: Клонировать репозиторий
        uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Чтобы получить всю историю

      - name: Показать список файлов
        run: ls -la

      - name: Получить версию и собрать скрипт
        run: |
          INPUT_SCRIPT="Beeline DMC Data Extractor.js"
          OUTPUT_SCRIPT="Beeline-DMC-Data-Extractor.auto.user.js"

          # Проверяем наличие файла
          if [ ! -f "$INPUT_SCRIPT" ]; then
            echo "❌ Ошибка: '$INPUT_SCRIPT' не найден!"
            exit 1
          fi

          # Читаем версию из оригинального файла
          VERSION=$(grep -oP '@version\\s+\\K[^ ]+' "$INPUT_SCRIPT")

          if [ -z "$VERSION" ]; then
            echo "❌ Не удалось прочитать версию"
            exit 1
          fi

          echo "📥 Найдена версия: $VERSION"

          # Генерируем .user.js
          cat > "$OUTPUT_SCRIPT" << EOF
          // ==UserScript==
          // @name         Beeline DMC Data Extractor + AutoUpdater
          // @namespace    http://tampermonkey.net/
          // @version      $VERSION
          // @description  Извлечение данных из Beeline DMC с автоапдейтером
          // @author       zOnVolga
          // @match        https://dmc.beeline.ru/*
          // @grant        GM_xmlhttpRequest
          // @grant        GM_notification
          // @grant        GM_getValue
          // @grant        GM_setValue
          // @connect      raw.githubusercontent.com
          // @connect      api.github.com
          // @downloadURL  https://raw.githubusercontent.com/zOnVolga/DMC_scripts/main/Beeline-DMC-Data-Extractor.auto.user.js
          // @updateURL    https://raw.githubusercontent.com/zOnVolga/DMC_scripts/main/Beeline-DMC-Data-Extractor.auto.user.js
          // @icon         https://raw.githubusercontent.com/zOnVolga/DMC_scripts/main/icon-beeline-yellow.svg
          // ==/UserScript==

          (function() {
              'use strict';

              // Загружаем оригинальный скрипт динамически
              const ORIGINAL_SCRIPT_URL = 'https://raw.githubusercontent.com/zOnVolga/DMC_scripts/main/Beeline%20DMC%20Data%20Extractor.js';

              // Подключаем внешний скрипт
              GM_xmlhttpRequest({
                  method: 'GET',
                  url: ORIGINAL_SCRIPT_URL,
                  onload: function(response) {
                      if (response.status === 200) {
                          const scriptContent = response.responseText;
                          eval(scriptContent); // Выполняем оригинальный скрипт
                      } else {
                          console.error('Не удалось загрузить оригинальный скрипт:', response.status);
                      }
                  },
                  onerror: function(err) {
                      console.error('Ошибка при загрузке оригинального скрипта:', err);
                  }
              });

              // === [Автоапдейтер] ===
              (function checkForUpdates() {
                  const scriptName = 'Beeline DMC Data Extractor';
                  const repoOwner = 'zOnVolga';
                  const repoName = 'DMC_scripts';
                  const filePath = 'Beeline%20DMC%20Data%20Extractor.js'; // URL-encoded filename
                  const rawUrl = 'https://raw.githubusercontent.com/' + repoOwner + '/' + repoName + '/main/' + filePath;
                  const apiUrl = 'https://api.github.com/repos/' + repoOwner + '/' + repoName + '/commits?path=' + encodeURIComponent(filePath) + '&page=1&per_page=1'

                  function getVersionFromString(content) {
                      const versionMatch = content.match(/\/\/\s*@version\s*([0-9.\-]+)/);
                      return versionMatch ? versionMatch[1].trim() : null;
                  }

                  function compareVersions(v1, v2) {
                      const parts1 = v1.split('.').map(Number);
                      const parts2 = v2.split('.').map(Number);
                      for (let i = 0; i < Math.max(parts1.length, parts2.length); i++) {
                          if ((parts1[i] || 0) > (parts2[i] || 0)) return 1;
                          if ((parts1[i] || 0) < (parts2[i] || 0)) return -1;
                      }
                      return 0;
                  }

                  // Проверка обновлений
                  GM_xmlhttpRequest({
                      method: 'GET',
                      url: rawUrl,
                      onload: function(response) {
                          if (response.status === 200) {
                              const remoteVersion = getVersionFromString(response.responseText);
                              const localVersion = GM_getValue('localVersion', '0.0.0');

                              if (!remoteVersion) {
                                  console.error('❌ Не найдена версия в удаленном скрипте');
                                  return;
                              }

                              console.log('[Автоапдейтер] Локальная версия:', localVersion);
                              console.log('[Автоапдейтер] Удаленная версия: ', remoteVersion);

                              if (compareVersions(remoteVersion, localVersion) > 0) {
                                  console.log(\`🔔 Доступна новая версия: \${remoteVersion}\`);

                                  fetch(apiUrl)
                                      .then(res => res.json())
                                      .then(commits => {
                                          if (commits.length > 0) {
                                              const commit = commits[0];
                                              const message = commit.commit.message;
                                              const author = commit.author?.login || commit.commit.author.name;
                                              const date = new Date(commit.commit.author.date).toLocaleString();

                                              GM_notification({
                                                  title: 'Доступно обновление',
                                                  text: \`\n\${scriptName} v\${remoteVersion}\n\n"${message}"\nАвтор: \${author}\nДата: \${date}\n→ Нажми, чтобы открыть и обновить\`,
                                                  timeout: 15,
                                                  onclick: () => window.open(rawUrl)
                                              });
                                          }
                                      })
                                      .catch(err => {
                                          console.error('Ошибка при получении информации о коммите:', err);
                                      });
                              } else {
                                  console.log(\`✅ Текущая версия актуальна: \${localVersion}\`);
                              }
                          }
                      },
                      onerror: function(err) {
                          console.error('❌ Ошибка при проверке обновления:', err);
                      }
                  });

                  // Сохраняем текущую версию из скрипта
                  const thisScriptVersion = getVersionFromString(document.currentScript.textContent);
                  if (thisScriptVersion && thisScriptVersion !== localVersion) {
                      GM_setValue('localVersion', thisScriptVersion);
                      console.log(\`💾 Локальная версия обновлена до: \${thisScriptVersion}\`);
                  }

                  setTimeout(checkForUpdates, 6 * 60 * 60 * 1000);
              })();
          })();
